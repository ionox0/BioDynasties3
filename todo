- is it currently a pattern that goal executors emit events that are handled elsewhere? or do AI goal executors just execute the goals?
- if executors emit events for other systems to handle, i beleive the pattern is for a common module from the /rts folder handles these events for both player / AI units. ensure this is the current pattern, and if so we may want to re-use the existing construction.rs module if it exists

- update claude rules, and document somewhere (maybe in root folder?) that shows this^ relationship between AI goals system, and common /rts modules. 
  - include in this doc that the AI goals and player-specifi systems should not directly call into common /rts modules, 
    nor should they modify state of entities directly,
    but instead should emit events that are handled by common /rts modules.

- update player building construction logic to flow thru common /rts construction system using events, instead of calling entity factor directly



Ideas:
- abstract the systems into an "RTS engine"
  - allow any models to be applied to units / buildings / resources, so we can create arbitrary RTS games
  - "space RTS" with spaceships, "medieval RTS" with knights and castles, etc

- needs a core strategy system that makes it more interesting / challenging
  - units have weaknesses / strengths against other units, so players need to build a variety of units to be effective

- AI should have different "strategies" that make them more aggressive, defensive, etc

- Make 2 AIs that play each other √
  - train a neural net to play the game, and have it play against itself to improve over time?

- Fog of War

- stop animations when in Idle state

- underground digging with map transparency

- tilt units based on terrain normals

- improve UI design

- fix AI not building / generating military units

- new buildings are not same size as initial ones - is base initialization using common building functions?

- random seed for map generation

- AI should not queue more goals for unit production than they have resources for

- 